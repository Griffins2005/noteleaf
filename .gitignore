# Dependencies
node_modules/

# Build outputs
dist/
.next/
out/
build/

# Turbo / TypeScript cache
.turbo/
*.tsbuildinfo

# Environment — never commit (only root .env + .env.production)
.env
.env.production
.env.local
.env.*.local

# Local deployment guide (Vercel + Render — not shared via git)
DEPLOY.local.md

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test coverage
coverage/

# OS
.DS_Store
Thumbs.db

# Keys / certs
*.pem

# Editors (personal settings only; keep shared config if added later)
.vscode/settings.json
.idea/

# Local AI / agent tooling
.claude

# Prisma local SQLite (not used in production, but ignore if created)
*.db
*.db-journal
